module.exports = {
  // Book filters array(non string types)
  nonStringBookFilters: ["enabled"],
  // Common filters array(skip, limit, sortBy, etc...)
  commonFilters: ["skip", "limit", "sortBy"],
};
